1) Подозрительный файл level11.lua
2) Если погуглить расширение этого файла, то оказывается это скрипт на языке Lua.
3) Можно посмотреть что там внутри:
cat level11.lua
Интересная строка io.popen("echo "..pass.." | sha1sum")
3) Запустим этот скрипт: lua: ./level11.lua:3: address already in use
stack traceback:
 [C]: in function 'assert'
 ./level11.lua:3: in main chunk
 [C]: ?c
4)Командой netstat -tulpn видно, что сервер прослушивает новые соединения по порту 5151 и локальному сетевому адрессу 127.0.0.1
5) Ну что ж попробуем проверить открыт ли 5151 порт с помощью nc -zv localhost 5151 (netcat - это простая утилита Unix, которая читает и записывает данные через сетевые соединения, используя протокол TCP или UDP):
Connection to localhost 515 port [tcp/pcrd] succeeded!
6) Отлично,  если еще раз вглянуть на скрипт, то видно, что пользавательская функция получает ввод полученный от клиента в виде пароля, а затем он вызвает процесс io.peopen. Эврика, а что если перенаправить свое вместо пароля клиента? То есть запускам nc localhost5151,  а когда попросят у нас пароль введем $(getflag > /tmp/flag).
7) Теперь посмотрим, что у нас получилось: cat /tmp/flag:
Check flah.Here is your token : fa6v5ateaw21peobuub8ipe6s
